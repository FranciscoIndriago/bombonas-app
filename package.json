{
  "name": "bombonas-app",
  "version": "0.1.0",
  "description": "App de Servicio de bombonas con JWT + PDF + Dark Mode",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  
}
